<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end">
            <a class="text-white p-2 w-32 bg-red-600 text-center mb-5 rounded-lg" href="/logout">logout</a>
        </div>
        <h2 class="mt-5">Hello <%= user.name %>
        </h2>
        <h3 class="mb-5">you can create a new post</h3>
        <form action="/post" method="post">
            <textarea class="resize-none bg-transparent w-1/3 outline-none border-2 border-zinc-800 p-3 rounded-md"
                name="content" placeholder="Post Content" required></textarea>
            <button class="bg-yellow-600 p-2 w-48 rounded-lg block mt-2" type="submit">Create Post</button>
        </form>

        <h4 class="text-3xl text-white my-4">your posts</h4>

        <div class="flex flex-col gap-4">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="bg-zinc-800 w-1/3 p-4 rounded-lg">

                    <span class="mb-7">
                        <%= user.username %>
                    </span>
                    <p class="text-white text-lg mt-2">
                        <%= post.content %>
                    </p>
                    <div class="flex gap-4 mt-4">
                        <a class="bg-transparent border-2 border-zinc-600 px-4 py-1 rounded-lg" href="">like</a>
                        <a class="bg-transparent border-2 border-zinc-600 px-4 py-1 rounded-lg" href="">edit</a>
                    </div>

                </div>
                <% }) %>
</body>

</html>